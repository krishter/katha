services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: katha
      POSTGRES_PASSWORD: katha
      POSTGRES_DB: katha
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://katha:katha@db:5432/katha
    depends_on:
      - db
    volumes:
      - ./backend:/app
    # --reload is for local dev only — the Dockerfile's own CMD (used in
    # production) deliberately omits it. --reload itself is single-process,
    # so it isn't combined with --workers here (uvicorn doesn't support
    # --reload with multiple workers regardless).
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      # Anonymous volumes so the container's own Linux-built node_modules/
      # (from `npm ci` in the Dockerfile) and .next cache aren't shadowed by
      # the host bind mount above — the host's node_modules contains macOS
      # native binaries (e.g. lightningcss, @next/swc) that don't load in
      # the Linux container.
      - /app/node_modules
      - /app/.next

volumes:
  postgres_data:
