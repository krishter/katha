# Git Workflow Rules

- Branch naming: `feature/<short-slug>`, `fix/<short-slug>`, `chore/<short-slug>`
- Commit messages: imperative mood, ≤72 chars, e.g. "Add story extraction schema"
- Always run tests before committing
- Never commit directly to `main`; open a PR
- PR title should match the primary commit message
